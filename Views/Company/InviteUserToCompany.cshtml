@model AppUser

@inject UserManager<AppUser> UserManager

@{
    AppUser appUser = (await UserManager.GetUserAsync(User))!;
}

<div class="pagetitle">
    <h1>Invite User</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">Company</li>
            <li class="breadcrumb-item active">Invite User</li>
        </ol>
    </nav>
</div>

<section class="section">
    <div class="row">

        <div class="col-6">

            @if (ViewData["Errors"] is not null)
            {
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    @ViewData["Errors"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            @if (ViewData["Message"] is not null)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    @ViewData["Message"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            <br/>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Invite a user to @ViewData["CompanyName"]</h5>

                    <p>Once the user has registered their e-mail address on the site, you can use it to invite them to your company.
                        After they accept, use the Manage Roles page under "Company" to assign them a role.</p>

                    <form class="row g-3 mt-3" method="post" asp-controller="Company" asp-action="InviteUserToCompany">
                        <div class="col-12">
                            <label asp-for="Email" class="form-label">Enter the user's email address:</label>
                            <input asp-for="Email" type="email" class="form-control">
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    </form>

                </div>
            </div>

        </div>
    </div>
</section>