@model Project

<div class="pagetitle">
	<h1>Archive Project</h1>
	<nav>
		<ol class="breadcrumb">
			<li class="breadcrumb-item">Projects</li>
			<li class="breadcrumb-item">Archived</li>
			<li class="breadcrumb-item active">Unarchive</li>
		</ol>
	</nav>
</div>

<div class="col col-lg-6">
	<div class="card">
		<div class="card-header card-title ms-3"><strong>Project: </strong>@Model.Name</div>
		<div class="card-body">
			<form asp-controller="Projects" asp-action="UnarchiveProjectConfirmed" method="post" class="mt-2">
				<input type="hidden" asp-for="Id" />
				<p>Are you sure you want to unarchive this project?</p>

				<button type="submit" class="btn btn-warning">Confirm</button>
				<a class="btn btn-outline-secondary" asp-controller="Projects" asp-action="ViewProject" asp-route-projectId="@Model.Id">Cancel</a>
			</form>
		</div>
	</div>
</div>