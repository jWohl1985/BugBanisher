@model Ticket

<div class="pagetitle">
	<h1>Archive Ticket</h1>
	<nav>
		<ol class="breadcrumb">
			<li class="breadcrumb-item">Ticket</li>
			<li class="breadcrumb-item active">Archive</li>
		</ol>
	</nav>
</div>

<div class="col col-lg-6">
	<div class="card">
		<div class="card-header card-title ms-3"><strong>Ticket: </strong>@Model.Title</div>
		<div class="card-body">
			<form asp-controller="Tickets" asp-action="ArchiveTicketConfirmed" method="post" class="mt-2">
				<input type="hidden" asp-for="Id" />
				<p>Are you sure you want to archive this ticket? It won't appear on open or completed ticket lists anymore. You can unarchive it later.</p>

				<button type="submit" class="btn btn-warning">Confirm</button>
				<a class="btn btn-outline-secondary" asp-controller="Tickets" asp-action="ViewTicket" asp-route-ticketId="@Model.Id">Cancel</a>
			</form>
		</div>
	</div>
</div>